#The config thread will load properties file,
#the file which included properties will be reload by configuration in the following.  
configProperties=dynamic-inventory.properties,dynamic-shoppingCart.properties,dynamic-monitorJvm.properties


#What time interval dose the thread reload the properties file above.Unit:second
#If configReloadPeriod great than 0 then launch the reload thread
configReloadPeriod=-1
